/**
 * @deprecated Import from `@/hooks/social` instead.
 * This shim exists for backward compatibility only.
 */
export {
  useActivity,
  useFollows,
  useFollowUser,
  useUnfollowUser,
  useIsFollowing,
  useLeaderboard,
  useLeaderboardHash,
  useSearchUsers,
} from "./social";
